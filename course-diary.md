A repository consists of working directory, staging area and the repositors pro

## Commands
- `git status` -- shows changes to the working directory and the status of the staging area
- `git add` -- add changes to the file to the staging area
- `git commit` -- bunddle the staged changes and commit them to the repository
- `git push` -- synchronizes changes (commits) **to** the remote
 
